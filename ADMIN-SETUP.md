# 관리자 모드 준비하기 — 딱 세 가지만 하시면 됩니다

홈페이지 사진·글을 직접 고치려면, 처음 한 번만 아래 세 가지를 해 주셔야 합니다.
**돈은 들지 않습니다. 카드 등록도 필요 없습니다.** 5~10분이면 끝납니다.

준비물: 인터넷이 되는 컴퓨터, 그리고 `uanchurch0607@gmail.com` 구글 계정 로그인.

---

## 1단계 — 로그인 방식 하나 켜기 (1분)

1. https://console.firebase.google.com 에 들어가 **uanchurch-1** 프로젝트를 누릅니다.
2. 왼쪽 메뉴에서 **빌드 → Authentication** 을 누릅니다.
3. **Sign-in method**(로그인 방법) 탭을 누릅니다.
4. 목록에서 **이메일/비밀번호** 를 누르고, **사용 설정**을 켠 뒤 **저장** 합니다.
   (그 아래 '이메일 링크' 는 꺼 두셔도 됩니다.)

> 지금은 이게 꺼져 있어서, 관리자 화면에서 비밀번호를 넣어도 "아직 준비가 안 됐어요" 라고 나옵니다.

## 2단계 — 관리자 계정 하나 만들기 (2분)

1. 같은 **Authentication** 화면에서 **Users**(사용자) 탭을 누릅니다.
2. **사용자 추가** 를 누릅니다.
3. 이메일에 **`uanchurch0607@gmail.com`** 을 그대로 넣습니다.
   (홈페이지가 이 주소를 미리 알고 있어서, 목사님은 **비밀번호만** 입력하시면 됩니다.)
4. 비밀번호는 **원하시는 것**으로 정합니다. 6자 이상이면 됩니다.
   → 이 비밀번호가 앞으로 홈페이지를 고칠 때 쓰는 비밀번호입니다.
5. 추가된 줄의 **사용자 UID** 를 복사해 둡니다.
   (`kJ3x...` 처럼 생긴 긴 글자입니다. 3단계에서 씁니다.)

> 비밀번호는 나중에 관리자 화면 안에서 직접 바꾸실 수 있습니다.

## 3단계 — "이 계정만 고칠 수 있다"는 규칙 넣기 (3분)

1. 왼쪽 메뉴에서 **빌드 → Firestore Database** 를 누르고, 위쪽 **규칙** 탭을 누릅니다.
2. 이 폴더의 **`firestore.rules`** 파일을 메모장으로 열어 **전체를 복사**합니다.
3. 복사한 내용에서 **`ADMIN_UID_HERE`** 를 찾아, 2단계에서 복사해 둔 **사용자 UID** 로 바꿉니다.
   (따옴표는 그대로 두고 안쪽 글자만 바꿉니다.)
4. 콘솔 규칙 편집기의 기존 내용을 **모두 지우고** 바꾼 내용을 **붙여넣기** 한 뒤 **게시**를 누릅니다.

> ⚠ **중요** — `firestore.rules` 에는 **중보기도요청 앱의 규칙도 함께** 들어 있습니다.
> 그래서 통째로 붙여넣으셔야 두 앱이 모두 정상 동작합니다.
> 홈페이지 규칙만 붙여넣으면 중보기도요청 앱이 멈춥니다.

---

## 다 되었는지 확인하는 법

1. 홈페이지 주소 뒤에 `/admin.html` 을 붙여 들어갑니다.
   → https://0607giusang-ship-it.github.io/gwangju-yuan-homepage/admin.html
2. 2단계에서 정한 비밀번호를 넣고 **들어가기** 를 누릅니다.
3. 아무 글이나 조금 고치고 **저장하기** 를 누릅니다.
   - "저장했습니다" 가 나오면 성공입니다.
   - "보안 규칙이 이 계정의 쓰기를 아직 허용하지 않습니다" 가 나오면 → 3단계의 UID 를 다시 확인해 주세요.
   - "아직 준비가 안 됐어요" 가 나오면 → 1단계를 안 하신 것입니다.
4. 홈페이지를 새로고침하면 고친 내용이 보입니다. **다시 배포할 필요가 없습니다.**

---

## 자주 묻는 것

**Q. 비밀번호가 홈페이지 파일 안에 들어 있나요?**
아니요. 어디에도 없습니다. 입력하신 비밀번호는 구글 인증 서버로만 가고, 맞는지는 서버가
판단합니다. 홈페이지 파일을 아무리 뜯어봐도 비밀번호는 나오지 않습니다.

**Q. 비밀번호를 바꾸고 싶어요.**
관리자 화면 맨 아래 **비밀번호 바꾸기** 에서 직접 바꾸시면 됩니다. 방금 로그인한 상태에서만
바뀌니, 창을 오래 열어 두셨다면 새로고침 후 다시 들어와서 바꿔 주세요.

**Q. 비밀번호를 잊어버렸어요.**
Firebase 콘솔 → Authentication → Users 에서 그 계정의 점 세 개 메뉴로 비밀번호를 다시
정하실 수 있습니다.

**Q. 인터넷이 안 되면 홈페이지가 빈 화면이 되나요?**
아니요. 인터넷이 안 되거나 서버에 문제가 생기면 **마지막으로 배포된 내용**이 그대로 보입니다.
고친 내용만 안 보일 뿐, 화면이 비지 않습니다.

**Q. 사진은 얼마나 큰 것까지 되나요?**
올리실 때 자동으로 줄여서 저장합니다. 아주 큰 사진(수천만 화소)은 "조금 작은 사진을 올려
주세요" 라고 나올 수 있는데, 그때는 휴대폰에서 한 번 편집(자르기)해서 올리시면 됩니다.
